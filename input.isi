programa
    numero a, b.
    texto t1.

    leia(a).
    leia(b).
    leia(t1).
    
    t1 := 1 + "adas".
    a :=  1 - 1.
    b := 1.

    se (2 >= 3 && 4 < 2) entao {
        escreva(b).
        escreva(b).
    } senao  {
        escreva(a).
    }

    enquanto (a > 2 || 4 < b && 1 > 2) {
        escreva(a).
        escreva(b).
    }


    escreva(a).
    escreva(b).    

fimprog.