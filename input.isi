programa
    numero a, b.
    texto t1.


    leia(a).
    leia(b).
    leia(t1).

    a := 1 + 2 * 3 / b.
    t1 := "aeaeae" + 1.
    t1 := 123eadsadwd.

    se (2 >= 3 && 4 < 2) entao {
        escreva(b).
        escreva(b).
    } senao  {
        escreva(a).
    }

    enquanto (a > 2 || 4 < b && 1 > 2) {
        escreva(a).
        escreva(b).
    }


    escreva(a).
    escreva(b).    

fimprog.