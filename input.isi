programa
    numero a, b, c.
    texto t1, t2.


    leia(a).
    leia(b).
    leia(t1).

    a := 1 + 2 * 3 / b.

    se (2 >= 3 && 4 < 2) entao {
        escreva(b).
        escreva(b).
    } senao  {
        escreva(a).
    }

    enquanto (a > 2 || 4 < b && 1 > 2) {
        escreva(a).
        escreva(b).
    }


    escreva(a).
    escreva(b).    

fimprog.